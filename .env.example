# Database Configuration
# For Local PostgreSQL (Development)
NODE_ENV=development
DB_HOST=localhost
DB_PORT=5432
DB_NAME=teachers_training
DB_USER=karthi
DB_PASSWORD=

# For Google Cloud SQL (Production)
# Uncomment these when deploying to production
# NODE_ENV=production
# INSTANCE_CONNECTION_NAME=project:region:instance
# DB_NAME=teachers_training
# DB_USER=app_user
# DB_PASSWORD=secure_password_here

# Authentication
JWT_SECRET=your-secret-key-here-change-in-production
JWT_EXPIRES_IN=86400

# Session Management
SESSION_TTL_HOURS=24
SESSION_CLEANUP_INTERVAL=3600000

# Application Settings
QUIZ_PASS_THRESHOLD=70
MAX_QUIZ_ATTEMPTS=2
NUDGE_INACTIVITY_HOURS=48

# WhatsApp Business API (Existing)
WHATSAPP_ACCESS_TOKEN=your_access_token_here
WHATSAPP_PHONE_NUMBER_ID=your_phone_number_id_here
WHATSAPP_VERSION=v17.0
WEBHOOK_VERIFY_TOKEN=your_verify_token_here

# Google Cloud Configuration (Existing)
GCP_PROJECT_ID=your_project_id
GCP_LOCATION=us-central1
GCP_KEYFILE=path/to/keyfile.json

# Neo4j Configuration (Existing)
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=password

# ChromaDB Configuration (Existing)
CHROMA_HOST=localhost
CHROMA_PORT=8000

# Integration Placeholders (Future)
MOODLE_API_URL=https://moodle.example.com/api
MOODLE_API_TOKEN=placeholder
ELIBRARY_API_URL=https://elibrary.example.com/api
ELIBRARY_API_KEY=placeholder

# LDAP Configuration (Future)
# LDAP_URL=ldap://ldap.example.com:389
# LDAP_BIND_DN=cn=admin,dc=example,dc=com
# LDAP_BIND_PASSWORD=ldap_password
# LDAP_SEARCH_BASE=ou=users,dc=example,dc=com